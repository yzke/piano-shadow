# Install this after requirements.txt from the official PyTorch CUDA index:
# python -m pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.2
piano-transcription-inference>=0.0.6
